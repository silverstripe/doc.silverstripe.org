// The current stable major version
export const DEFAULT_VERSION = '6';
// The highest major version (may be unreleased, otherwise will be the current stable major version)
export const HIGHEST_VERSION = '6';
// The oldest major version shown
export const MINIMUM_VERSION = '3';
export const SITE_URL = process.env.SITE_URL || 'https://docs.silverstripe.org';
